{
  "name": "evidencias-pos-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API REST para sincronizar Evidencias POS con PostgreSQL.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "pg": "^8.13.3"
  }
}
